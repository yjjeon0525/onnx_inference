models:
  - name: "org"
    path: "D:/History/D22_4K/finetune_model/rear_qhd_512x256.onnx"
    type: "yolov8"
    nms_applied: false
    conf_threshold: null
    iou_threshold: null
    class_thresholds: null

preprocess:
  crop:
    enabled: true
    region: [0.0, 0.0, 1.0, 0.88888888888888888888888888888889]
  resize:
    method: "INTER_LINEAR"
    input_size: null

postprocess:
  conf_threshold: 0.25
  iou_threshold: 0.45
  class_thresholds: {}

output:
  save_video: true
  save_json: false
  display: false
  output_dir: "output/"

comparison:
  mode: "overlay"
  metrics: ["cosine_similarity", "precision", "recall", "inference_time"]

class_names: null

video:
  codec: "mp4v"
  preserve_fps: true
